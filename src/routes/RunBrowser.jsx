export default function RunBrowser() {
  return (
    <div className="p-4 text-muted">
      <h2 className="text-text text-sm font-semibold mb-2">Runs</h2>
      <p>Run browser stub — will list backtest run folders + live archives once wired.</p>
    </div>
  );
}
