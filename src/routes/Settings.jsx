export default function Settings() {
  return (
    <div className="p-4 text-muted">
      <h2 className="text-text text-sm font-semibold mb-2">Settings</h2>
      <p>Settings stub — env vars, relay creds, theme selection.</p>
    </div>
  );
}
